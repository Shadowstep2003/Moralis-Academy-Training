This project stems from the Javascript 101 course on IvanOnTech Moralis

You can auto create the structure of an html file by typing exclamation point then tab       ! > followed by tab

When incorporating images and needing to adjust css only set EITHER the width or height as not doing so messes with aspect ratio and scaling on different viewports. 